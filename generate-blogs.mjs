import fs from 'fs';
import path from 'path';

const outDir = path.join(process.cwd(), 'src/content/blogs');

console.log(`Blog generation script updated to prevent programmatic low-quality stub generation.`);
